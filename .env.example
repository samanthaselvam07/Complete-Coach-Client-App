# Public app URL used for client-facing links in deployed environments.
VITE_PUBLIC_APP_URL="https://client.completecoach.fit"

# Future API base URL for client-scoped Complete Coach endpoints.
# Leave empty while the app is using local mock data.
VITE_CLIENT_API_URL=""
